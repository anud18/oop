/* Copyright (c) 1999 by The National Chengchi Univ. All Rights Reserved */

/***************************************************************************
   NAME
     Cards.cc
   NOTE
     You are asked to design this module.
     You need to draw the given number of cards and print them out.
     Assume that you are given a deck of 52 cards. The cards do not reappear.
     That is, you do not put a card back into the deck after it is drawn.
***************************************************************************/
/*const int kNPip=13;
const int kNSuit=4;
const int kNCards=52;
const int kCardWidth=11;
const int kCardHeight=11;
const int pip = 1;
const int suit = 1;
bool take[52];
int* getCard(int cardNumber, int seed){
       srand(seed);
        int *p = new int[cardNumber];
        while(cardNumber > 0){
                int r = rand() % 52;
                while(!take[r]){
                        p[cardNumber--] = r;
                        take[r] = true;
                }
        }	
		return p;
}
pair<int, int> convert(int )
int main(){
	int cards[52][pip][suit];
	bool take[kNPip][kNSuit];
	int cardNumber;


}*/
